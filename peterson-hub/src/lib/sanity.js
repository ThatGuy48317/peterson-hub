// 1. Install sanity client: npm install @sanity/client
// 2. Un-comment the code below once you have your Project ID

/*
import { createClient } from '@sanity/client'

export const client = createClient({
  projectId: 'your_project_id', // Find this at manage.sanity.io
  dataset: 'production',
  useCdn: true, // set to `false` to bypass the edge cache
  apiVersion: '2023-05-03', // use current date (YYYY-MM-DD) to target the latest API version
})
*/

export const placeholder = "Sanity setup required";